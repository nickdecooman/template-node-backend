version: '2'
services:
  node:
    image:
      $NAME:$VERSION
    build:
      context: ../app
      dockerfile: Dockerfile
      args:
        NODE_ENV: production
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
